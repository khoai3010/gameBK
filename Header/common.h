#pragma once
#include <iostream>
#include <string>
#include <random>
#include <fstream>
#include "math.h"
#include "modul.h"
#include "map.h"

enum ModulType
{
	NONE,
	TREE = 12,
	HOUSE,
	CAR,
	GOTO,
};
